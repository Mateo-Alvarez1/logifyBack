Spring:
  datasource:
    url: jdbc:mysql://sfo1.clusters.zeabur.com:31146/zeabur
    username: root
    password: ${sqlPassword}
    driver-class-name: com.mysql.cj.jdbc.NonRegisteringDriver
  jpa:
    hibernate:
      ddl-auto: create-drop
      show-sql: true
      properties:
        hibernate:
          format_sql: true
      database: mysql
